class IIC_Command:
	def __init__(self, name, command, data):
		self.name = name
		self.command = command
		self.data = data
	
	def setValue(value):
		self.data = value
		
	def setCommand(command):
		self.command = command